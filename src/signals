# -*- shell-script -*-

################################################################################

function bashrun.signal.usr1 {

    local command
    bashrun.separator "SIGUSR1" 13

    bashrun.remote.run
}

trap bashrun.signal.usr1 SIGUSR1

################################################################################
