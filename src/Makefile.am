bin_SCRIPTS = bashrun

objdir = $(datadir)/@PACKAGE@
obj_DATA = actions bashrc bindings bookmarks bootstrap command completion config core engine frontend geometry globals handlers message modes object objects plugin progress readline registry remote rules terminals utils window xdotool

